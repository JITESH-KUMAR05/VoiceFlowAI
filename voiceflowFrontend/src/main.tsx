import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";
// we are going to develop one application which solves sales problem  in real world
createRoot(document.getElementById("root")!).render(<App />);
